---
services:
  inkfra:
    image: ddosian/inkfra:latest
    volumes:
      - ./config:/config:ro
      - ./output:/output:rw
    restart: unless-stopped
  nginx:
    image: nginx:latest
    volumes:
      - ./output:/usr/share/nginx/html:ro
    ports:
      - 80:80
    restart: unless-stopped
